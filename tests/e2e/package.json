{
  "name": "hnvr-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "HNVR end-to-end UI tests via Playwright (S4 deliverable, see design_docs/10-test-plan.md)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.5.0"
  }
}
