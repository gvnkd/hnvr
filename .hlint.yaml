# HLint hints configuration. hlint is run via the pre-commit-hooks.nix
# hook. The codebase has been cleaned up to satisfy ormolu 0.8 + hlint
# 3.10's default suggestions (redundant `id`/parens, `when (not X)` →
# `unless`, single-field `data` → `newtype`, unused pragmas, etc.), so
# this file currently overrides nothing. Keep it as an explicit allow-list
# anchor: future drift can be opt-in via entries here rather than silent.
